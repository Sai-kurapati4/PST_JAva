class Solution {
    public boolean rotateString(String s, String goal) {
        if(s.length()!=goal.length()){
            return false;
        }
        String g=goal+goal;
        if(g.contains(s)){
            return true;
        }
        else{
            return false;
        }
    }
}
